# Codebase Health Report 2026-09-21

## Tech Debt aus TASKS.md

Der Backlog zeigt eine betraechtliche Anzahl offener Tech-Debt-Items ueber alle Projekte und Prioritaetsstufen hinweg.

*   **P0 - Sofort (Sicherheit + Rechtlich):** Es gibt 5 kritische Sicherheitspunkte, die sofortige Aufmerksamkeit erfordern. Dazu gehoeren die Rotation von API-Keys fuer Gemini, PageSpeed und CRON_SECRET, die Rotation des GSC Service-Account-Keys sowie die Rotation von RESEND_API_KEY und ADMIN_PASSWORD fuer sq-schmidt. Fuer Halveo stehen noch Smoke-Tests auf Produktion und der Eintrag der Telefonnummer im Impressum aus.
*   **P1 - Diese Woche (Kunden + SEO):** Dieser Bereich umfasst zahlreiche offene Aufgaben, insbesondere fuer `meyso-website`. Hierzu gehoeren ausstehende SQL-Migrationen (z.B. 20260814_kontaktpersonen.sql und die Migrationen fuer Tempo 4/5), die damit verbundenen Rauchtests nach dem Deploy, die anwaltliche Pruefung von AGB und Vertragstexten sowie die Freischaltung des Vertragsversands. Weitere wichtige Punkte sind die Behebung des Kaltstartproblems im Admin-Bereich, die Implementierung einer Ratenbegrenzung fuer den Outreach-Versand und die DSGVO-Pruefung der Crawler-Kandidatenliste. Fuer Halveo sind ein Anwaltstermin fuer Legal-Reviews und das Stripe Customer Portal Branding offen.
*   **P2 - Naechste 2 Wochen (Tech Debt + Hardening):** In dieser Kategorie sind unter anderem die Integration der Finom Banking API (oder ein CSV-Import), die Evaluierung neuer Claude Code Features und Gemini API-Einstellungen sowie die Behebung von 19 `npm audit` Vulnerabilities in `hirmax` gelistet. Die Follow-ups zur Migration von Services-Daten nach Sanity, die Erstellung eines Sanity Read Tokens und die Konfiguration des `CRON_SECRET` sind ebenfalls offen. Ein kritischer Punkt ist die Untersuchung unvollstaendiger SEO-Agent-Daten seit Mai. Rechtlich stehen 6 AVVs (Vercel, Supabase, Resend, Sanity, Meyso-Hirmax) und DSGVO-Dokumente fuer Hirmax aus.
*   **P3 - Feature Backlog (Kundenprojekte):** Dieser Bereich enthaelt viele Features fuer Halveo (z.B. tax_rules Infrastruktur, NKA-Modul, Anlage V Export), Hirmax (Payment Processing), Toolradar (Blog-Generator, Dead Tool Detector) und Meyso-Website (Client-Uebersicht, Wartungs-Dashboard). Auch Infrastruktur- und DevEx-Aufgaben wie das Hirmax Order Monitoring und die Toolradar Content Generation sind hier zu finden. Fuer `meyso-kmu-template` ist der Build rot, und es fehlen Admin-Seiten fuer FAQ/Leistungen sowie die Kennzeichnung des Analyse-Widgets auf `meyso.de`.

## Lint Status (meyso-website)

Der bereitgestellte Lint Output zeigt, dass `npm run lint` mit einem Fehlercode endete. Es werden keine spezifischen Fehler oder Warnungen aufgelistet, die auf Linting-Verstoesse hinweisen. Der Fehler koennte mit einem `npm link`-Problem oder einer allgemeinen Konfigurationsschwierigkeit zusammenhaengen. Es ist jedoch zu beachten, dass eine fruehere Aufgabe zur Bereinigung von 214 Lint Errors (hauptsaechlich `@typescript-eslint/no-explicit-any`) auf `meyso-website` als erledigt markiert wurde.

## Audit Status (meyso-website)

Der `npm audit` Output meldet eine **High Severity Vulnerability** in der Abhaengigkeit `undici`. Die Schwachstelle wird als "HTTP response queue poisoning via keep-alive socket reuse" beschrieben. Ein Fix ist ueber `npm audit fix` verfuegbar.

## Empfehlung

Der dringendste Schritt ist die Behebung der P0-Sicherheitsprobleme, insbesondere die Rotation der API-Keys und die Behebung der `undici` High-Severity-Vulnerability mittels `npm audit fix`. Parallel dazu sollten die ausstehenden SQL-Migrationen fuer `meyso-website` eingespielt und die damit verbundenen Rauchtests durchgefuehrt werden, um die Stabilitaet der Finanz- und Admin-Systeme zu gewaehrleisten.