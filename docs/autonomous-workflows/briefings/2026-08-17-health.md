# Codebase Health Report 2026-08-17

## Tech Debt aus TASKS.md

Der Backlog listet eine beträchtliche Anzahl offener technischer Schulden und Entwicklungsaufgaben.

**P0 - Sofort (Sicherheit + Rechtlich)**: Es gibt 7 kritische Aufgaben, die sofortige Aufmerksamkeit erfordern. Dazu gehören das Rotieren mehrerer kompromittierter API-Keys (Gemini, PageSpeed, CRON_SECRET, GSC Service-Account-Key, sq-schmidt RESEND_API_KEY und ADMIN_PASSWORD), das Eintragen der Telefonnummer im Halveo-Impressum und Smoke-Tests für Halveo.

**P1 - Diese Woche (Kunden + SEO)**: Hier sind 10 Aufgaben gelistet, die direkten Kunden- oder Traffic-Impact haben. Wichtige Punkte sind der Anwalt-Termin für Halveo (AGB, AVV, Datenschutz), das Branding des Stripe Customer Portals, das Konfigurieren von Social Media API Keys, ein Lexware Export Test, sowie diverse Migrationen, Rauchtests und Funktionstests für die meyso-website.

**P2 - Naechste 2 Wochen (Tech Debt + Hardening)**: Dieser Bereich umfasst 16 Aufgaben, die interne Aufräumarbeiten und Systemhärtung betreffen. Dazu gehören die Evaluierung einer Finom Banking API Integration, die Nutzung neuer Claude Code Features, das Setzen von Gemini API Spend Caps, die Verifikation von Halveo UI-Elementen, die Erstellung eines DATEV-Exports, die Anfrage eines externen Sicherheits-Audits und die Dokumentation von Newsletter Secrets. Ein großer Block sind die rechtlichen AVV-Dokumente für Hirmax und Meyso.

**P3 - Feature Backlog (Kundenprojekte)**: Eine umfangreiche Liste von Feature-Entwicklungen für Halveo (z.B. NKA-Modul, Anlage V Export, Mietvertrag-Generator, Mieter-Portal, Beleg-OCR mit Position-Level-Extraktion), Hirmax (Payment Processing), toolradar (Blog-Generator, Dead Tool Detector) und die meyso-website (Client-Übersicht, Wartungs-Dashboard). Auch Infrastructure/DevEx-Aufgaben wie die Migration auf pnpm und Turborepo sind hier gelistet.

**Affiliate-Roadmap (Halveo)**: Die Roadmap ist in vier Phasen unterteilt und enthält 17 Aufgaben, die sich auf den Aufbau eines Affiliate-Programms konzentrieren, von manuellen Coupons bis hin zu einem Tier-System in Rewardful.

**GEPARKT und LANGFRISTIG**: Diese Sektionen enthalten strategische und zukünftige Aufgaben, die derzeit keine Priorität haben, wie das Steuerberater-Feature für Halveo, automatische Vertragsgenerierung, ein SEO-Dashboard (bereits teilweise umgesetzt) und Skalierungsmaßnahmen wie Multi-Tenant-Architektur und automatisierte Tests.

## Lint Status (meyso-website)

Der bereitgestellte Lint Output zeigt keine spezifischen Lint-Fehler oder Warnungen. Stattdessen handelt es sich um eine Fehlermeldung von `npm` bezüglich des `npm link` Befehls und einer Aufforderung, `npm run` auszuführen, um eine Liste der Skripte zu sehen. Daher kann der aktuelle Lint-Status der `meyso-website` aus diesem Output nicht abgeleitet werden.

## Audit Status (meyso-website)

Der `npm audit` Output für die `meyso-website` zeigt eine kritische Sicherheitslücke:

*   **1 High Severity Vulnerability**: `undici` ist anfällig für HTTP response queue poisoning via keep-alive socket reuse.
    *   Details: `https://github.com/advisories/GHSA-35p6-xmwp-9g52`
    *   Betroffenes Modul: `node_modules/undici`
    *   Fix verfügbar via `npm audit fix`.

## Empfehlung

Angesichts der kritischen P0-Aufgaben und der identifizierten High-Severity-Sicherheitslücke sollte der nächste sinnvolle Schritt sein, umgehend alle API-Keys zu rotieren, die als kompromittiert gelten, und die `undici`-Schwachstelle mittels `npm audit fix` zu beheben. Parallel dazu sollten die Halveo P0-Aufgaben (Daniel-Mail, Smoke-Test, Impressum Telefonnummer) abgeschlossen werden, um rechtliche und operative Risiken zu minimieren.