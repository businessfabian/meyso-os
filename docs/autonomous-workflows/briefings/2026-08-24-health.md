# Codebase Health Report 2026-08-24

## Tech Debt aus TASKS.md
*   **P0 - Sofort (Sicherheit + Rechtlich)**: 6 offene Tasks. Kritische Sicherheitsthemen wie API-Key-Rotationen (Gemini, PageSpeed, CRON_SECRET, GSC Service-Account, sq-schmidt Credentials) und rechtliche Pflichten (Halveo Impressum, Daniel-Mail, Smoke-Test) sind dringend.
*   **P1 - Diese Woche (Kunden + SEO)**: 11 offene Tasks. Der Fokus liegt auf kundenrelevanten und traffic-bringenden Themen, darunter rechtliche Reviews für Halveo, Stripe Customer Portal Branding, Tests für Halveo, sowie Sanity CORS, Google Business Profile und Social Media API Keys für Meyso-Projekte. Auch Dave's manuelle SQL-Migration und Rauchtest für meyso-website sind hier gelistet.
*   **P2 - Nächste 2 Wochen (Tech Debt + Hardening)**: 31 offene Tasks. Hier geht es um interne Aufräumarbeiten und Systemhärtung, wie die Implementierung eines DATEV-Exports für Halveo, die Evaluierung von Claude Code Features, Gemini API Spend Caps, die Migration von Services-Daten nach Sanity, das Erzwingen von Quality Gates, sowie zahlreiche rechtliche AVV-Aktivierungen und die Dokumentation von TOMs für Hirmax.
*   **P3 - Feature Backlog (Kundenprojekte)**: 60 offene Tasks. Dies umfasst die Weiterentwicklung von Halveo (Objekte, Beleg-Scan, Finanz-Cockpit, Mieter-PWA, etc.), die Fertigstellung von Hirmax Payment Processing, Toolradar-Features (Blog-Generator, Price Monitor), Meyso-Website-Erweiterungen (Client-Übersicht, Wartungs-Dashboard) und Infrastructure/DevEx-Verbesserungen.

Insgesamt sind 108 Tasks in den Prioritäten P0 bis P3 offen.

## Lint Status (meyso-website)
Der Lint-Befehl für `meyso-website` scheint nicht erfolgreich durchgelaufen zu sein. Der Output zeigt `npm error` Meldungen bezüglich `npm link` und der Aufforderung, `npm run` auszuführen, anstatt tatsächliche Lint-Fehler oder Warnungen. Ein detaillierter Lint-Status kann daher nicht ermittelt werden.

## Audit Status (meyso-website)
Es wurde 1 High-Severity-Schwachstelle gefunden:
*   `undici` ist anfällig für HTTP response queue poisoning via keep-alive socket reuse (https://github.com/advisories/GHSA-35p6-xmwp-9g52).
Eine Behebung ist über `npm audit fix` verfügbar.

## Empfehlung
Priorisiere diese Woche die verbleibenden P0-Items, insbesondere die API-Key-Rotationen und das Eintragen der Telefonnummer im Impressum, um kritische Sicherheits- und Rechtsrisiken zu minimieren. Parallel dazu sollte `npm audit fix` ausgeführt werden, um die identifizierte High-Severity-Schwachstelle zu beheben.