# Codebase Health Report 2026-04-13

## Tech Debt aus TASKS.md

Die offenen technischen Schulden sind nach Priorität geordnet.

**🔴 P0 - Sofort (Sicherheit + Rechtlich)**
Hier stehen kritische Aufgaben wie die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET), die Prüfung und Aktivierung von Auftragsverarbeitungsverträgen (AVVs) für Vercel, Sanity, Resend, sowie die Erstellung eines AVV zwischen Meyso und Hirmax. Diese sind als echtes Risiko eingestuft.

**🟠 P1 - Diese Woche (Kunden + SEO)**
In dieser Kategorie finden sich Aufgaben mit direktem Kunden- oder Traffic-Impact. Dazu gehören ein dringendes Next.js Update auf eine gepatchte Version wegen einer DoS-Schwachstelle (CVE-2026-23869), die Anlage von Hirmax als Kunde in Sanity, die Prüfung der Sanity CORS-Einstellungen, Google Business Profile-Pflege, Konfiguration von Social Media API Keys, End-to-End-Tests des Lexware-Exports und die realistische Festlegung von Wartungsvertrags-Reaktionszeiten. Mehrere UI-Quick Wins für `villa-nina`, `toolradar` und `hirmax` aus dem UI Modernization Audit stehen ebenfalls aus.

**🟡 P2 - Nächste 2 Wochen (Tech Debt + Hardening)**
Diese Aufgaben haben keinen direkten externen Impact, dienen aber der Codebase-Pflege und -Härtung. Dazu gehören die Integration der Finom Banking API oder ein CSV-Import für Admin Finanzen, die Evaluierung neuer Claude Code Features (`/powerup`, Monitor-Tool), Gemini API-Optimierungen (Spend Cap, neue Flash-Preview, Flex Inference Tier), ein Next.js 16.2 Update, die Evaluierung der Supabase Stripe Sync Engine für `hirmax`, das Aufräumen von Lint Errors in `meyso-website` (hauptsächlich `@typescript-eslint/no-explicit-any`), `npm audit fix` für `hirmax` und die Entfernung von `.env.local` aus der Git-History von `sq-schmidt-website`.

**🟢 P3 - Feature Backlog (Kundenprojekte)**
Hier sind wertschöpfende Features für Kundenprojekte gelistet, darunter Kunden-Self-Service und Payment Processing für `hirmax-scheibenbilder`, Blog- und Newsletter-Tests für `toolradar`, sowie verschiedene Dashboard-Erweiterungen für `meyso-website` (Client-Übersicht, Wartungs-Dashboard, Provision Wizard). Auch Infrastruktur- und DevEx-Aufgaben wie manuelle Trigger Buttons für Workflows und C: Space Cleanup sind hier zu finden.

## Lint Status (meyso-website)

Der Lint Output für `meyso-website` zeigt:
*   **148 Errors**
*   **15 Warnings**
*   **Insgesamt 163 Probleme**

Die größte Problemkategorie, die im Backlog erwähnt wird, ist `@typescript-eslint/no-explicit-any`, die in vielen Dateien auftritt. Es gibt auch eine Warnung für eine ungenutzte Variable (`writeFileSync`) in `generate-og-image.mjs`.

## Audit Status (meyso-website)

Der `npm audit` Output für `meyso-website` zeigt:
*   **0 vulnerabilities found**

Dies ist ein sehr guter Status, es wurden keine bekannten Sicherheitslücken in den Abhängigkeiten gefunden.

## Empfehlung

Der nächste sinnvolle Schritt für diese Woche ist die sofortige Abarbeitung der **P0-Aufgaben**, insbesondere die API-Key-Rotation und AVV-Prüfungen, da diese ein echtes Sicherheits- und Rechtsrisiko darstellen. Parallel dazu sollte das dringende **Next.js Update wegen CVE-2026-23869 (P1)** priorisiert werden, um die DoS-Schwachstelle zu beheben. Anschließend wäre es ratsam, die **148 Lint Errors in `meyso-website`** anzugehen, um die Codequalität und Wartbarkeit zu verbessern.