# Codebase Health Report 2026-06-22

## Tech Debt aus TASKS.md
Die offenen technischen Schulden in TASKS.md lassen sich in folgende Hauptkategorien unterteilen:

**P0 - Sofort (Sicherheit + Rechtlich):**
*   **Halveo:** Ausstehende Kundenkommunikation (Daniel-Mail), Produktion Smoke-Tests, Impressum-Ergänzungen.
*   **Meyso-Projekte:** Dringende Rotation kritischer API-Keys (Gemini, PageSpeed, CRON_SECRET, Resend, Admin-Passwort für sq-schmidt).

**P1 - Diese Woche (Kunden + SEO):**
*   **Halveo:** Rechtliche Überprüfung von AGB/AVV/Datenschutz, Branding des Stripe Customer Portals, Schreiben von Unit-Tests.
*   **Meyso-Projekte:** Konfiguration und Prüfung von Sanity CORS für Hirmax, Google Business Profile-Pflege, Social Media API Keys, End-to-End-Test des Lexware Exports, realistische Festlegung von Wartungsvertrag-Reaktionszeiten.

**P2 - Nächste 2 Wochen (Tech Debt + Hardening):**
*   **Halveo:** Schema-Erweiterungen, manuelle UI-Verifikation, DATEV-Export-Aufbau, Anfrage für externes Sicherheits-Audit.
*   **Meyso-Projekte:** Integration der Finom Banking API (oder CSV-Import), Evaluierung und Dokumentation neuer Claude Code Features, Konfiguration von Gemini API Spend Caps und Test neuer Modelle, Evaluierung von Supabase Stripe Sync, Dokumentation von Newsletter Secrets, Fortsetzung der Sanity-Migration für Services-Daten, Überprüfung des Quality Gates für toolradar, Erstellung eines separaten Sanity Read Tokens, Setzen von CRON_SECRET auf Vercel, `npm audit fix` für hirmax, Evaluierung von pnpm und Turborepo für DevEx-Optimierung, Nutzung von Vercel Env Var Groups, Aufbau einer Client-Systeme-Sektion im Admin Dashboard.
*   **Rechtliches Hirmax:** Erstellung von Verarbeitungsverzeichnis und TOMs durch Max, sowie Aktivierung und Erstellung von fünf AVVs (Vercel, Supabase, Resend, Sanity, Meyso-Hirmax) durch Dave.

## Lint Status (meyso-website)
Der Lint-Befehl für `meyso-website` konnte nicht ausgeführt werden, da `eslint` nicht gefunden wurde (`sh: 1: eslint: not found`). Der aktuelle Lint-Status der Codebasis ist daher unbekannt.

## Audit Status (meyso-website)
Der `npm audit` für `meyso-website` zeigt insgesamt 29 Schwachstellen an:
*   **1 kritische** Schwachstelle
*   **7 hohe** Schwachstellen
*   **20 moderate** Schwachstellen
*   **1 niedrige** Schwachstelle

Die kritische und die hohen Schwachstellen stellen ein erhebliches Sicherheitsrisiko dar und sollten umgehend adressiert werden.

## Empfehlung
Priorisiere diese Woche die Rotation der kritischen API-Keys (P0) und führe `npm audit fix --force` für `meyso-website` durch, um die 29 gefundenen Schwachstellen, insbesondere die kritische und die hohen, zu beheben. Parallel dazu sollte die Ursache für den fehlenden `eslint`-Befehl untersucht und behoben werden, um den Lint-Status zukünftig überwachen zu können.