# Codebase Health Report 2026-08-31

## Tech Debt aus TASKS.md

Die offenen technischen Schulden konzentrieren sich auf folgende Bereiche:

*   **P0 - Sofort (Sicherheit + Rechtlich):** Mehrere kritische API-Key-Rotationen stehen aus (Gemini, PageSpeed, CRON_SECRET, GSC Service Account, Resend, Admin Password für sq-schmidt). Zudem sind für Halveo rechtliche Schritte wie das Eintragen der Telefonnummer im Impressum und Smoke-Tests auf Produktion dringend.
*   **P1 - Diese Woche (Kunden + SEO):** Für Halveo sind Anwaltstermine für AGB, AVV und Datenschutz sowie das Branding des Stripe Customer Portals offen. Bei den Meyso-Projekten müssen Sanity CORS für Hirmax geprüft, Google Business Profile vervollständigt und Social Media API Keys konfiguriert werden. Zwei wichtige Dave-Tasks für meyso-website sind die Supabase-Migration und ein Rauchtest nach dem Deploy.
*   **P2 - Naechste 2 Wochen (Tech Debt + Hardening):** Hier sind strukturelle Verbesserungen für Halveo (Schema-Erweiterung, DATEV-Export, Sicherheits-Audit-Anfrage) und Meyso-Projekte (Finom Banking API Integration, Claude Code Exploration, Gemini API Spend Caps/Evaluation, Sanity Read Token, CRON_SECRET) gelistet. Besonders hervorzuheben ist das Problem, dass der SEO-Agent seit Mai unvollständige Daten liefert. Ein umfassender Block widmet sich zudem der Erstellung von DSGVO-Dokumenten und AVVs für Hirmax und Meyso.

## Lint Status (meyso-website)

Der bereitgestellte Lint Output zeigt keine spezifischen Lint-Fehler oder Warnungen, sondern lediglich allgemeine `npm error` Meldungen bezüglich `npm link` und `npm run`. Dies deutet darauf hin, dass der Lint-Prozess selbst möglicherweise nicht korrekt ausgeführt wurde oder fehlerhaft beendet wurde, anstatt tatsächliche Code-Qualitätsprobleme zu melden. Ein detailliertes Logfile ist unter `/home/runner/.npm/_logs/2026-08-31T16_17_36_921Z-debug-0.log` verfügbar.

## Audit Status (meyso-website)

Der npm audit Output identifiziert eine kritische Sicherheitslücke:

*   **1 High Severity Vulnerability:** `undici` ist anfällig für HTTP response queue poisoning via keep-alive socket reuse.
*   Ein Fix ist via `npm audit fix` verfügbar.

## Empfehlung

Die höchste Priorität für diese Woche sollte auf den P0-Sicherheitstasks liegen, insbesondere der Rotation aller kompromittierten oder kritischen API-Keys. Direkt danach sollte die Behebung der High Severity Vulnerability in `undici` mittels `npm audit fix` erfolgen, um das Risiko von HTTP response queue poisoning zu eliminieren.