# Codebase Health Report 2026-06-01

## Tech Debt aus TASKS.md

Der `TASKS.md` Backlog listet zahlreiche offene technische Schulden und Features quer durch die Projekte.

**P0 - Sofort (Sicherheit + Rechtlich)**:
Die dringendsten Aufgaben betreffen die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET) und die ausstehenden Credentials für `sq-schmidt` (RESEND_API_KEY, ADMIN_PASSWORD). Für Halveo stehen ein Smoke-Test auf Produktion und das Eintragen der Telefonnummer im Impressum an.

**P1 - Diese Woche (Kunden + SEO)**:
Prioritäre Punkte, die direkten Kunden- oder SEO-Impact haben, umfassen die Buchung eines Anwaltstermins für Halveo (AGB, AVV, Datenschutz), das Branding des Stripe Customer Portals und das Schreiben von Unit-Tests für Halveo (H-3 + H-4). Für Meyso-Projekte sind das Prüfen der Sanity CORS für Hirmax, das Konfigurieren des Google Business Profiles und der Social Media API Keys sowie ein End-to-End-Test des Lexware Exports wichtig.

**P2 - Naechste 2 Wochen (Tech Debt + Hardening)**:
In dieser Kategorie finden sich Aufgaben wie die Erweiterung des Halveo C-5 Schemas, die UI-Verifikation von H-6 + H-1, das Aufsetzen des DATEV-Exports und die Anfrage eines externen Sicherheits-Audits. Für Meyso-Projekte stehen die Evaluierung der Finom Banking API für Admin Finanzen, das Testen neuer Claude Code Features, das Setzen von Gemini API Spend Caps und die Evaluierung neuer Gemini Modelle an. Des Weiteren müssen die `npm audit fix` für Hirmax durchgeführt, die pnpm/Turborepo-Migration evaluiert und eine Client-Systeme-Sektion im Admin Dashboard gebaut werden. Eine Reihe von AVVs (Vercel, Supabase, Resend, Sanity, Meyso-Hirmax) sowie die Dokumentation der TOMs und des Verarbeitungsverzeichnisses für Hirmax sind ebenfalls offen.

## Lint Status (meyso-website)

Der bereitgestellte Lint Output zeigt die Meldung `sh: 1: eslint: not found`. Dies bedeutet, dass ESLint auf dem System, auf dem der Lint-Befehl ausgeführt wurde, nicht gefunden oder nicht korrekt installiert ist. Aktuell können daher keine Code-Qualitätsprüfungen oder Fehleranalysen für `meyso-website` durchgeführt werden.

## Audit Status (meyso-website)

Der `npm audit` für `meyso-website` identifiziert insgesamt 24 Sicherheitslücken:
*   **1 Critical** vulnerability
*   **2 High** vulnerabilities
*   **21 Moderate** vulnerabilities

Diese Schwachstellen stellen ein erhebliches Sicherheitsrisiko dar. Insbesondere die kritische und die beiden hohen Schwachstellen sollten umgehend behoben werden, da sie potenziell schwerwiegende Auswirkungen auf die Sicherheit und Stabilität der Anwendung haben könnten. Der Bericht schlägt vor, `npm audit fix` oder `npm audit fix --force` auszuführen.

## Empfehlung

Der nächste sinnvolle Schritt für diese Woche ist die sofortige Behebung der 1 kritischen und 2 hohen npm-Sicherheitslücken in `meyso-website` mittels `npm audit fix --force`, nach sorgfältiger Prüfung möglicher Breaking Changes. Parallel dazu sollte die Ursache für die `eslint: not found`-Meldung für `meyso-website` untersucht und behoben werden, um die Code-Qualitätskontrolle wiederherzustellen.