# Codebase Health Report 2026-09-07

## Tech Debt aus TASKS.md

Der Backlog weist weiterhin eine hohe Anzahl offener Tech-Debt-Items auf, mit signifikantem Fortschritt in der "meyso-website: Belegkette" und dem Kundenportal, aber auch neuen, nachgelagerten Aufgaben.

**🔴 P0 - Sofort (Sicherheit + Rechtlich)**: Es sind 6 kritische Aufgaben offen. Dazu gehören die Rotation von API-Keys für Gemini, PageSpeed, CRON_SECRET und den GSC Service-Account-Key, sowie die Rotation von RESEND_API_KEY und ADMIN_PASSWORD für sq-schmidt nach einem Leak. Im Halveo-Projekt stehen ein Smoke-Test auf Produktion und das Eintragen der Telefonnummer im Impressum an.

**🟠 P1 - Diese Woche (Kunden + SEO)**: Insgesamt sind 24 Aufgaben in dieser Priorität offen. Ein großer Teil davon entfällt auf die "meyso-website: Belegkette", wo nach umfangreichen Fertigstellungen (V0a-V2, AGB, Zusagen, V4 Kundenportal, V5 Aenderungsanfragen, V6 Zubuchen, V7 Ausland) noch 16 Nachlauf- und Folgeaufgaben anstehen, darunter die anwaltliche Prüfung von AGB und Vertragstexten, die Implementierung von V3 (Wiederkehrende Rechnungen) und V10/V11 (E-Rechnungen/Regelbesteuerung) sowie die Einrichtung eines lokalen Supabase-Stacks für bessere Testbarkeit. Weitere P1-Aufgaben umfassen den Anwalt-Termin für Halveo (Legal-Review AGB/AVV/Datenschutz), Stripe Customer Portal Branding, Sanity CORS-Prüfung für Hirmax und die Konfiguration von Social Media API Keys.

**🟡 P2 - Nächste 2 Wochen (Tech Debt + Hardening)**: Hier sind 28 Aufgaben gelistet. Dazu gehören Halveo-spezifische Punkte wie das C-5 Folge-Schema und der DATEV-Export, sowie allgemeine Tech-Debt-Items wie die Integration der Finom Banking API, die Evaluierung neuer Claude Code Features und Gemini API Tiers, die Erstellung eines separaten Sanity Viewer-Tokens und die Behebung unvollständiger Daten des SEO-Agenten. Ein wichtiger Block sind die 6 ausstehenden AVV-Aktivierungen und -Erstellungen für Hirmax.

## Lint Status (meyso-website)

Der bereitgestellte Lint-Output zeigt einen `npm error` beim Versuch, ein Skript auszuführen, anstatt eine Liste von Lint-Fehlern. Dies deutet auf ein Problem beim Aufruf des Lint-Befehls hin, nicht auf den Lint-Status selbst. Es ist jedoch zu beachten, dass laut TASKS.md die Aufgabe `[x] 🤖 Claude | meyso-website: 214 Lint Errors aufraeumen (hauptsaechlich @typescript-eslint/no-explicit-any)` als erledigt markiert ist. Der aktuelle Lint-Status kann daher nicht direkt aus dem Output abgeleitet werden.

## Audit Status (meyso-website)

Der `npm audit` Output zeigt eine kritische Sicherheitslücke:

*   **1 High Severity Vulnerability**:
    *   `undici vulnerable to HTTP response queue poisoning via keep-alive socket reuse - https://github.com/advisories/GHSA-35p6-xmwp-9g52`
    *   Betroffen: `node_modules/undici`

Es wird empfohlen, `npm audit fix` auszuführen, um dieses Problem zu beheben.

## Empfehlung

Die höchste Priorität sollte auf die Behebung der **1 High Severity Vulnerability** mittels `npm audit fix` und die Abarbeitung der **6 P0-Sicherheits- und Rechtsaufgaben** gelegt werden, insbesondere die Rotation aller kompromittierten API-Keys. Parallel dazu sollte die Ursache des `npm error` im Lint-Output untersucht werden, um den Code-Qualitäts-Check wiederherzustellen.