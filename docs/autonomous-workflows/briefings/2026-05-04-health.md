# Codebase Health Report 2026-05-04

## Tech Debt aus TASKS.md

Der Codebase Health Report zeigt eine signifikante Anzahl offener technischer Schulden und Entwicklungsaufgaben, die sich über verschiedene Prioritätsstufen und Projekte verteilen.

**P0 - Sofort (Sicherheit + Rechtlich):**
Die höchste Priorität liegt auf kritischen Sicherheits- und rechtlichen Aufgaben. Dazu gehören das Versenden einer Beta-Mail und Smoke-Tests für Halveo, das Eintragen der Telefonnummer im Impressum, sowie das Rotieren von API-Keys (Gemini, PageSpeed, CRON_SECRET) und spezifischen Credentials für `sq-schmidt` nach einem Leak.

**P1 - Diese Woche (Kunden + SEO):**
Für diese Woche sind Aufgaben geplant, die direkten Kunden- oder Traffic-Impact haben. Bei Halveo stehen ein Anwaltstermin für Legal-Review, Stripe Customer Portal Branding und das Schreiben von Unit-Tests an. Projektübergreifend müssen Hirmax in Sanity angelegt und dessen CORS geprüft werden, Google Business Profile aktualisiert, Social Media API Keys konfiguriert, der Lexware Export getestet und realistische Wartungsvertrag-Reaktionszeiten festgelegt werden.

**P2 - Nächste 2 Wochen (Tech Debt + Hardening):**
In den nächsten zwei Wochen stehen interne Aufräum- und Härtungsmaßnahmen an. Für Halveo sind dies Schema-Erweiterungen, UI-Verifikationen, DATEV-Export und die Anfrage eines externen Sicherheits-Audits. Für Meyso-Projekte sind Aufgaben wie die Finom Banking API Integration, die Evaluierung neuer Claude Code Features und Gemini API-Einstellungen, die Evaluierung von Supabase Stripe Sync, das Setzen von Secrets (Newsletter, CRON_SECRET), die Erstellung eines Sanity Read Tokens und die Bereinigung von `hirmax` npm audit Fehlern sowie die Korrektur des `package.json` Namens relevant. Ein großer Block sind auch die rechtlichen AVVs für Vercel, Supabase, Resend, Sanity und der AVV zwischen Meyso und Hirmax.

**P3 - Feature Backlog (Kundenprojekte):**
Der Feature Backlog umfasst zahlreiche wertschöpfende Aufgaben für Halveo (z.B. Objekte-Feature, Beleg-Scan, Finanz-Cockpit, Mieter-PWA, Halveo Doku), Hirmax (Payment Processing), toolradar (Blog-Generator, Newsletter Integration) und die meyso-website (Client-Übersicht, Wartungs-Dashboard). Auch Infrastructure/DevEx-Aufgaben wie manuelle Trigger Buttons und weitere autonome Loops sind hier gelistet.

**Geparkt und Langfristig:**
Längerfristige und geparkte Aufgaben umfassen die Steuerberater-Integration für Halveo (abhängig von echtem Steuerberater-Input), die automatische Vertragsgenerierung und ein SEO-Dashboard. Langfristig sind automatisierte Tests, Performance Monitoring, Error Tracking und Zod Validation für alle API-Routes sowie Multi-Tenant-Architektur und weitere Feature-Erweiterungen für die Meyso-Plattform vorgesehen.

## Lint Status (meyso-website)

Der ESLint-Befehl konnte nicht ausgeführt werden, da `eslint` nicht gefunden wurde. Daher kann kein Lint-Status für `meyso-website` berichtet werden.

## Audit Status (meyso-website)

Der `npm audit` für `meyso-website` zeigt insgesamt 24 Sicherheitslücken:
*   **1 kritische** Schwachstelle
*   **23 moderate** Schwachstellen

Es wird empfohlen, `npm audit fix` auszuführen, um die meisten Probleme zu beheben, und `npm audit fix --force` für alle Probleme, einschließlich potenzieller Breaking Changes. Die kritische Schwachstelle stellt ein unmittelbares Risiko dar und sollte priorisiert behandelt werden.

## Empfehlung

Angesichts der kritischen Sicherheitslücken (P0-Tasks und die 1 kritische npm audit Schwachstelle) sollte der Fokus diese Woche auf der Behebung dieser liegen. Konkret: API-Keys rotieren, `sq-schmidt` Credentials sichern, die Halveo P0-Tasks abschließen und `npm audit fix --force` auf `meyso-website` ausführen, um die kritische Schwachstelle zu beheben.