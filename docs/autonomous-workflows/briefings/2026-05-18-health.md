# Codebase Health Report 2026-05-18

## Tech Debt aus TASKS.md
Der `TASKS.md` Backlog listet eine Reihe offener technischer Schulden und Features, die nach Prioritaet geordnet sind:

*   **P0 - Sofort (Sicherheit + Rechtlich)**: Kritische Aufgaben umfassen die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET), das Beheben von geleakten Credentials bei sq-schmidt (RESEND_API_KEY, ADMIN_PASSWORD), das Eintragen der Telefonnummer im Impressum von Halveo sowie Smoke-Tests auf Halveo Production.
*   **P1 - Diese Woche (Kunden + SEO)**: Hier stehen Aufgaben mit direktem Kunden- oder Traffic-Impact an, wie die Buchung eines Anwalt-Termins fuer Halveo (AGB, AVV, Datenschutz), das Branding des Stripe Customer Portals, das Anlegen von Hirmax als Kunde in Sanity und die Pruefung der Sanity CORS-Einstellungen. Auch die Konfiguration von Social Media API Keys und der end-to-end Test des Lexware Exports sind hier gelistet.
*   **P2 - Naechste 2 Wochen (Tech Debt + Hardening)**: Dieser Bereich konzentriert sich auf interne Verbesserungen und Haertung. Dazu gehoeren das Hinzufuegen einer Spalte zum Halveo C-5 Schema, die UI-Verifikation von Halveo H-6 und H-1, die Aufsetzung eines DATEV-Exports, die Anfrage eines externen Sicherheits-Audits sowie die Integration der Finom Banking API oder eines CSV-Imports fuer Admin Finanzen. Weiterhin sind die Evaluierung von Claude Code Powerups und Monitor-Tool, Gemini API Spend Caps und Tiers, die Erstellung eines separaten Sanity Viewer-Tokens, das Setzen des CRON_SECRET auf Vercel, `npm audit fix` fuer hirmax und die Erstellung diverser AVVs fuer Hirmax und Meyso-Subunternehmer (Vercel, Supabase, Resend, Sanity) wichtige Punkte.
*   **P3 - Feature Backlog (Kundenprojekte)**: Dieser Bereich enthaelt wertschaffende Features fuer Kundenprojekte. Fuer Halveo sind dies unter anderem die `tax_rules` Infrastruktur, das NKA-Modul, Anlage V Export, Objekte-Feature, Beleg-Scan und Finanz-Cockpit. Fuer hirmax-scheibenbilder steht die Payment Processing Integration aus. Fuer toolradar sind Blog-Generator, Newsletter Integration und Dead Tool Detector geplant. Fuer die meyso-website sind Client-Uebersicht, Wartungs-Dashboard und Provision Wizard vorgesehen. Infrastruktur-Aufgaben wie manuelle Trigger-Buttons fuer Workflows und Space Cleanup sind ebenfalls enthalten.
*   **GEPARKT / LANGFRISTIG**: Diese Abschnitte enthalten Aufgaben, die derzeit keine hohe Prioritaet haben oder von externen Faktoren abhaengen, wie das Steuerberater-Feature fuer Halveo, die automatische Vertragsgenerierung oder ein SEO-Dashboard. Langfristig sind Themen wie automatisierte Tests, Performance Monitoring, Multi-Tenant Architekturen und Internationalisierung geplant.

## Lint Status (meyso-website)
Der Lint-Befehl `eslint` wurde nicht gefunden. Daher kann kein Status bezueglich Lint-Fehlern oder Warnungen fuer `meyso-website` berichtet werden.

## Audit Status (meyso-website)
Der `npm audit` fuer `meyso-website` hat insgesamt 8 Sicherheitsluecken identifiziert:
*   5 moderate
*   2 high
*   1 critical

Die kritische Schwachstelle stellt ein hohes Risiko dar und sollte priorisiert untersucht und behoben werden.

## Empfehlung
Angesichts der kritischen Sicherheitsluecke im `npm audit` und der offenen P0-Aufgaben, die sich auf Sicherheit und rechtliche Compliance konzentrieren, sollte diese Woche die Behebung der kritischen `npm audit` Schwachstelle sowie die Rotation der API-Keys und das Beheben der geleakten Credentials bei sq-schmidt priorisiert werden. Dies sind die dringendsten Schritte, um potenzielle Sicherheitsrisiken zu minimieren und die rechtliche Konformitaet sicherzustellen.