# Codebase Health Report 2026-06-29

## Tech Debt aus TASKS.md

Der `TASKS.md` Backlog, zuletzt aktualisiert am 2026-04-30, listet folgende offene technische Schulden:

*   **P0 - Sofort (Sicherheit + Rechtlich):** Es gibt 5 kritische Aufgaben, die sich auf die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET, Resend, Admin-Passwort), das Eintragen einer Telefonnummer im Impressum und Smoke-Tests für Halveo konzentrieren. Diese sind als sofortiges Risiko eingestuft.
*   **P1 - Diese Woche (Kunden + SEO):** 8 Aufgaben sind offen, darunter die Buchung eines Anwaltstermins für Halveo (AGB, AVV, Datenschutz), Stripe Customer Portal Branding, das Schreiben von Tests für Halveo-Funktionen sowie die Konfiguration von Sanity CORS, Google Business Profile und Social Media API Keys für Meyso-Projekte.
*   **P2 - Naechste 2 Wochen (Tech Debt + Hardening):** Mit 22 offenen Punkten ist dies der größte Block an technischen Schulden. Er umfasst Aufgaben wie die Erweiterung des Halveo-Schemas, die UI-Verifikation mit Echtdaten, die Einrichtung eines DATEV-Exports, die Evaluierung von Finom Banking API und neuen Claude/Gemini-Features, sowie umfangreiche rechtliche Schritte für Hirmax (Verarbeitungsverzeichnis, TOMs, diverse AVVs).
*   **P3 - Feature Backlog (Kundenprojekte):** Eine Vielzahl von Features für Halveo, Hirmax, Toolradar, meyso-website und Infrastructure/DevEx sind gelistet. Für Halveo stehen unter anderem Objekte-Feature, Beleg-Scan, Finanz-Cockpit und die Halveo-Doku an.
*   Zusätzlich gibt es eine detaillierte Affiliate-Roadmap, GEPARKTE Items (z.B. Steuerberater-Feature, Automatische Vertragsgenerierung, SEO-Dashboard) und LANGFRISTIGE Ziele (z.B. Automatisierte Tests, Multi-Tenant Architektur).

## Lint Status (meyso-website)

Der Lint-Output für `meyso-website` zeigt an, dass `eslint` nicht gefunden wurde (`sh: 1: eslint: not found`). Daher kann der aktuelle Lint-Status, einschließlich der Anzahl der Fehler und Warnungen oder der größten Problemkategorien, nicht ermittelt werden.

## Audit Status (meyso-website)

Der `npm audit` Output für `meyso-website` meldet insgesamt 30 Sicherheitslücken:

*   **1 Critical**
*   **8 High**
*   20 Moderate
*   1 Low

Die kritische und die acht hohen Sicherheitslücken stellen ein erhebliches Risiko dar und sollten umgehend untersucht und behoben werden, da sie potenziell schwerwiegende Auswirkungen auf die Sicherheit und Stabilität des Projekts haben können.

## Empfehlung

Angesichts der kritischen und hohen Sicherheitslücken im `meyso-website` Projekt sowie der offenen P0-Aufgaben im `TASKS.md` (insbesondere API-Key-Rotation), sollte die oberste Priorität diese Woche der Behebung der Sicherheitsrisiken gelten. Dies beinhaltet das Ausführen von `npm audit fix --force` auf `meyso-website` nach sorgfältiger Prüfung der Änderungen und die Rotation aller kritischen API-Keys.