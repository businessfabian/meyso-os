# Codebase Health Report 2026-05-11

## Tech Debt aus TASKS.md

Der `TASKS.md` Backlog zeigt eine Reihe offener technischer Schulden und geschäftskritischer Aufgaben, priorisiert nach Dringlichkeit:

*   **P0 - Sofort (Sicherheit + Rechtlich)**: Hier stehen kritische Aufgaben wie die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET, Resend, Admin-Passwort für sq-schmidt), das Eintragen der Telefonnummer im Impressum für Halveo und Smoke-Tests auf Halveo Production. Diese sind als sofortiges Risiko gekennzeichnet.
*   **P1 - Diese Woche (Kunden + SEO)**: Dieser Bereich umfasst Aufgaben, die direkten Kunden- oder Traffic-Impact haben. Dazu gehören ein Anwalt-Termin für Halveo (AGB, AVV, Datenschutz), Stripe Customer Portal Branding, das Schreiben von Unit-Tests für Halveo (AfA-Calculator, Anlage V), die Prüfung von Sanity CORS für Hirmax, die Konfiguration von Social Media API Keys und ein end-to-end Test des Lexware Exports.
*   **P2 - Nächste 2 Wochen (Tech Debt + Hardening)**: Hier finden sich interne Aufräum- und Härtungsmaßnahmen. Offen sind unter anderem die Erweiterung des Halveo C-5 Schemas, manuelle UI-Verifikation für Halveo, die Aufsetzung des DATEV-Exports, die Anfrage eines externen Sicherheits-Audits, die Integration der Finom Banking API oder ein CSV-Import für Admin Finanzen, die Evaluierung neuer Claude Code Features und Gemini API Tiers, das Setzen von Spend Caps für Gemini, die Evaluierung von Supabase Stripe Sync für Hirmax, die Dokumentation des Newsletter Secrets, die Erzwingung eines Quality Gates für toolradar, die Erstellung eines separaten Sanity Viewer-Tokens, das Setzen des CRON_SECRET auf Vercel, ein `npm audit fix` für hirmax, die Evaluierung von pnpm/Turborepo sowie die Erstellung einer Client-Systeme Section im Admin Dashboard. Zudem sind noch alle AVVs (Vercel, Supabase, Resend, Sanity, Meyso-Hirmax) und das Verarbeitungsverzeichnis/TOMs für Hirmax offen.
*   **P3 - Feature Backlog (Kundenprojekte)**: Dieser Bereich enthält wertschöpfende Features für Kundenprojekte, die nach den dringenderen Tech-Debt-Aufgaben angegangen werden sollen. Dazu gehören diverse Halveo-Features (Objekte-CRUD, Beleg-Scan, Finanz-Cockpit, Mieter-PWA, Kehrwoche, Netatmo-Integration, Architektur-Refactor, Email-Encoding, Kategorie-Sync, Bucket-Konsolidierung), die Implementierung von Payment Processing für Hirmax, sowie diverse Features für toolradar, meyso-website und Infrastructure/DevEx (z.B. manuelle Trigger Buttons für Workflows, Hirmax Order Monitoring Loop, toolradar Content Generation Loop, C: Space Cleanup, pnpm store auf D: verlegen).
*   **Affiliate-Roadmap**: Die Phase 1 des Affiliate-Programms ist noch nicht abgeschlossen, es fehlt die initiale Excel-Tabelle für das Tracking und die Festlegung der Standard-Provision.
*   **GEPARKT / LANGFRISTIG**: Diese Aufgaben sind derzeit nicht aktiv im Fokus und werden zu einem späteren Zeitpunkt wieder evaluiert.

## Lint Status (meyso-website)

Der Lint-Befehl für `meyso-website` konnte nicht ausgeführt werden. Die Ausgabe `sh: 1: eslint: not found` deutet darauf hin, dass ESLint im Ausführungspfad nicht gefunden wurde oder nicht korrekt installiert ist. Dies ist ein kritischer Zustand, da ohne Linting die Codequalität und Konsistenz nicht automatisiert überprüft werden kann.

## Audit Status (meyso-website)

Der `npm audit` für `meyso-website` zeigt insgesamt **10 Sicherheitslücken**:
*   **1 kritische** Schwachstelle
*   **1 hohe** Schwachstelle
*   **8 moderate** Schwachstellen

Die Existenz einer kritischen und einer hohen Schwachstelle stellt ein erhebliches Sicherheitsrisiko dar und sollte umgehend adressiert werden.

## Empfehlung

Für diese Woche ist es entscheidend, die **P0-Aufgaben** (insbesondere die Rotation der API-Keys und die Halveo Smoke-Tests) sowie die **kritische und hohe Sicherheitslücke** im `npm audit` zu beheben. Parallel dazu sollte das **ESLint-Problem** auf `meyso-website` umgehend gelöst werden, um die Codequalität wieder sicherzustellen.