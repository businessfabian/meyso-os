# Codebase Health Report 2026-06-15

## Tech Debt aus TASKS.md

Die offenen technischen Schulden sind umfangreich und verteilen sich über mehrere Prioritätsstufen und Projekte.

**P0 - Sofort (Sicherheit + Rechtlich)**:
Dringende Aufgaben umfassen die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET) für Meyso-Projekte und die Rotation spezifischer Credentials (RESEND_API_KEY, ADMIN_PASSWORD) für sq-schmidt. Für Halveo stehen ein Daniel-Mail Versand, Smoke-Tests auf Produktion und die Eintragung der Telefonnummer im Impressum an.

**P1 - Diese Woche (Kunden + SEO)**:
Hierzu zählen für Halveo die Buchung eines Anwalt-Termins für Legal-Review, Stripe Customer Portal Branding und das Schreiben von Unit-Tests für H-3 + H-4. Meyso-Projekte müssen Hirmax in Sanity anlegen, CORS prüfen, Google Business Profile aktualisieren, Social Media API Keys konfigurieren, den Lexware Export testen und Wartungsvertrag-Reaktionszeiten realistisch setzen.

**P2 - Naechste 2 Wochen (Tech Debt + Hardening)**:
Dieser Bereich umfasst Halveo-Aufgaben wie die Erweiterung des C-5 Folge-Schemas, UI-Verifikation mit Realdaten, DATEV-Export und die Anfrage eines externen Sicherheits-Audits. Für Meyso-Projekte stehen die Integration der Finom Banking API oder ein CSV-Import, die Evaluierung neuer Claude Code Features, die Konfiguration von Gemini API Spend Caps und die Migration auf `gemini-3-flash-preview` an. Weitere Punkte sind die Dokumentation des Newsletter Secrets, die Prüfung des Quality Gates für toolradar, die Erstellung eines separaten Sanity Read Tokens, das Setzen des CRON_SECRET auf Vercel, die Behebung von `npm audit` Problemen für hirmax und die Evaluierung von pnpm/Turborepo. Rechtlich sind für Hirmax das Verarbeitungsverzeichnis und TOMs zu erstellen, sowie fünf AVVs (Vercel, Supabase, Resend, Sanity, Meyso-Hirmax) zu aktivieren.

**P3 - Feature Backlog (Kundenprojekte)**:
Der Feature Backlog ist sehr umfangreich und beinhaltet unter anderem die Implementierung von `tax_rules`, einem NKA-Modul und Anlage V Export für Halveo, Payment Processing für hirmax-scheibenbilder, sowie Blog-Generator und Dead Tool Detector für toolradar. Für die meyso-website sind Client- und Wartungs-Dashboards geplant. Infrastruktur-seitig sind manuelle Trigger für Workflows, weitere Space Cleanups und die Migration von pnpm Caches auf D: vorgesehen. Für halveo sind zudem zahlreiche weitere Features wie Objekte-CRUD, Beleg-Scan, Finanz-Cockpit, Mieter-PWA, Netatmo-Integration und eine umfassende Dokumentation auf der Roadmap.

**Affiliate-Roadmap (Halveo)**:
Die Affiliate-Roadmap befindet sich in Phase 1, dem manuellen Coupon-Modus mit Excel-Tracking und manueller Provision. Die Phasen 2 bis 4 beinhalten die Einführung von Rewardful, Affiliate-Sektionen, Outreach-Aktivitäten und ein Tier-System.

**GEPARKT / LANGFRISTIG**:
Längerfristige und geparkte Aufgaben umfassen das Steuerberater-Feature für Halveo, die automatisierte Vertragsgenerierung, ein SEO-Dashboard, sowie die Implementierung von automatisierten Tests, Performance Monitoring, Structured Logging und Zod Validation für alle Projekte.

## Lint Status (meyso-website)

Der Lint-Prozess für `meyso-website` konnte nicht ausgeführt werden, da der `eslint` Befehl nicht gefunden wurde. Es konnten daher keine Lint-Fehler oder Warnungen ermittelt werden. Dies deutet auf ein Problem in der Entwicklungsumgebung oder der Projektkonfiguration hin, das die Ausführung des Linters verhindert.

## Audit Status (meyso-website)

Der `npm audit` für `meyso-website` hat folgende Schwachstellen identifiziert:

*   **Gesamt**: 22 Schwachstellen
*   **Kritisch**: 1
*   **Hoch**: 9
*   **Moderat**: 12

Es wird empfohlen, `npm audit fix` auszuführen, um Probleme zu beheben, die keine Aufmerksamkeit erfordern. Für alle Probleme, einschließlich potenziell brechender Änderungen, kann `npm audit fix --force` verwendet werden.

## Empfehlung

Der nächste sinnvolle Schritt für diese Woche ist die sofortige Behebung der kritischen und hoch-priorisierten Sicherheitslücken, die durch `npm audit` in `meyso-website` aufgedeckt wurden. Parallel dazu sollte die Ursache für den `eslint: not found` Fehler untersucht und behoben werden, um die Code-Qualitätssicherung wiederherzustellen.