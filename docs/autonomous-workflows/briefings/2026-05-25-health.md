# Codebase Health Report 2026-05-25

## Tech Debt aus TASKS.md

Die offenen technischen Schulden verteilen sich auf verschiedene Prioritätsstufen und Projekte:

*   **P0 - Sofort (Sicherheit + Rechtlich)**: Hier stehen kritische Sicherheitsthemen im Vordergrund. Dazu gehören die Rotation von API-Keys (Gemini, PageSpeed, CRON_SECRET, Resend, Admin Password für sq-schmidt), das Eintragen der Telefonnummer im Impressum von Halveo und Smoke-Tests auf der Halveo-Produktion.
*   **P1 - Diese Woche (Kunden + SEO)**: Diese Kategorie umfasst Aufgaben mit direkter Kunden- oder SEO-Relevanz. Offen sind hier Anwaltstermine für Halveo (Legal-Review), Branding des Stripe Customer Portals, das Schreiben von Unit-Tests für Halveo (AfA-Calculator, Anlage V Mapping), die Anlage von Hirmax als Kunde in Sanity, die Prüfung der Sanity CORS-Einstellungen für Hirmax, Google Business Profile-Updates, die Konfiguration von Social Media API Keys, End-to-End-Tests des Lexware Exports und die realistische Festlegung von Wartungsvertrag-Reaktionszeiten.
*   **P2 - Naechste 2 Wochen (Tech Debt + Hardening)**: Diese Aufgaben betreffen die interne Codebase und das System-Hardening. Für Halveo sind dies Schema-Erweiterungen, UI-Verifikationen, das Aufsetzen des DATEV-Exports und die Anfrage eines externen Sicherheits-Audits. Projektübergreifend stehen die Finom Banking API Integration, die Evaluierung neuer Claude Code-Features und Gemini API-Einstellungen (Spend Cap, Flash-Preview, Flex Inference Tier), die Erwähnung des Newsletter Secrets im README des kmu-templates, die Migration weiterer Daten nach Sanity (partnersData, certificatesData), die Erzwingung des Quality Gates für toolradar, die Erstellung eines separaten Sanity Read Tokens, das Setzen des CRON_SECRET auf Vercel, die Behebung von 19 `npm audit` Vulnerabilities bei hirmax, das Aufräumen von 214 Lint Errors auf der meyso-website, die Evaluierung von pnpm/Turborepo, Vercel Env Var Groups und der Bau einer Client-Systeme-Sektion im Admin-Dashboard an. Ein signifikanter Block sind die rechtlichen Aufgaben für Hirmax und Meyso, darunter die Erstellung von Verarbeitungsverzeichnissen und TOMs sowie die Aktivierung von fünf Auftragsverarbeitungsverträgen (AVVs).
*   **P3 - Feature Backlog (Kundenprojekte)**: Hier sind noch zahlreiche Features für Halveo (Objekte-Feature, Beleg-Scan, Finanz-Cockpit, Mieter-PWA, etc.), die hirmax-scheibenbilder-Website (Payment Processing), toolradar (Blog-Generator, Newsletter Integration, Dead Tool Detector, Price Monitor, DSGVO-Report) und die meyso-website (Client-Übersicht, Wartungs-Dashboard, Provision Wizard, Cockpit) offen. Auch Infrastruktur-Aufgaben wie manuelle Trigger-Buttons für Workflows, Hirmax Order Monitoring Loop und toolradar Content Generation Loop sind hier gelistet.

## Lint Status (meyso-website)

Der Lint Output für `meyso-website` zeigt an, dass `eslint` nicht gefunden wurde (`sh: 1: eslint: not found`). Dies bedeutet, dass die Linting-Prüfung nicht ausgeführt werden konnte. Es gibt daher keine Informationen über die Anzahl der Errors oder Warnings oder die größten Problemkategorien. Dies ist ein kritischer Zustand, da die Code-Qualität nicht automatisiert überwacht wird.

## Audit Status (meyso-website)

Der `npm audit` Output für `meyso-website` identifiziert insgesamt 24 Sicherheitslücken:
*   **1 kritische** Schwachstelle
*   **2 hohe** Schwachstellen
*   **21 moderate** Schwachstellen

Die kritische und die hohen Schwachstellen stellen ein erhebliches Sicherheitsrisiko dar und sollten umgehend adressiert werden.

## Empfehlung

Die höchste Priorität für diese Woche sollte auf den **P0-Sicherheitsaufgaben** liegen, insbesondere der **Rotation aller API-Keys** und der **Behebung der kritischen und hohen npm audit Schwachstellen** auf der meyso-website. Parallel dazu muss die **ESLint-Konfiguration für meyso-website** umgehend korrigiert werden, um die Code-Qualität wieder sicherzustellen.